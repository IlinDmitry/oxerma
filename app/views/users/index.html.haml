.container{style: 'margin-top: 20px;'}
  .row
    .col-sm-6
      %h1 Пользователи
.container{style: 'margin-top: 20px;'}
  .row
    - @users.each do |user|
      - user = UserPresenter.new user
      .col-md-6{style: 'margin-top: 30px;'}
        .media
          .square-box.pull-left
            = image_tag user.image_small_thumb_path, size:'72x72', class: 'rounded-circle'
          .media-body
            %div{style: 'padding-left: 10px;'}
              %p
                %b= link_to user.email, user
              %p= Role::TYPE_EXTERNALS[user.roles.first.name.to_sym].to_s.capitalize